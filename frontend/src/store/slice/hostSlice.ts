import { createSlice } from "@reduxjs/toolkit";

const initialState = {

};

const hostslice  = createSlice({
  name: "hosts",
  initialState,
  reducers: {},
});

export const {

} = hostslice.actions;

export default hostslice.reducer;